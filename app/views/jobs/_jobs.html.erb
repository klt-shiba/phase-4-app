<% jobs.each do | job |%>
  <li>
    <div><b>
        <h3><%=link_to job.title, user_job_path(job_poster(job).id, job.id)%></b></h3>
      <h4>- <%=job_poster(job).username.capitalize%> will pay $<%=job.price%></h4>
    </div>
  </li>
<%end%>
